# quic

Package quic provides a QUIC API for the Media over QUIC (MoQ) protocol.
It does not implement the QUIC protocol itself, but rather provides a wrapper around the `quic-go/quic-go`
library as `quicgo`.
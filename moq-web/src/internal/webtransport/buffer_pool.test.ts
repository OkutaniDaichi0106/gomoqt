// Tests migrated to the Deno standard testing library and renamed to
// `buffer_pool_test.ts`. Keep this stub in place during staged migration to
// avoid duplicate-symbol diagnostics while both the old and new tests exist.

export {};

# webtransport

Package webtransport provides a WebTransport API for the Media over QUIC (MoQ) protocol.
It does not implement the WebTransport protocol itself, but rather provides a wrapper around the `quic-go/webtransport-go`
library as `webtransportgo`.